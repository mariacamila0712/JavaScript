const numeros = [10, 55, 108];
console.log(numeros);
const amigos = new Array('Pedro', 'Juan', 'Diego');
console.log(amigos);

// Un Arreglo puede contener cualquier tipo de datos en Javascript
const arregloFull = ["Mouse", 50, false, "not", null, {nombre: 'Miguel'}];
console.log(arregloFull);

const productos = ['Televisor', 'Mouse', 'Notebook', 'Reloj'];

// for loop
for( let i = 0; i<productos.length; i++) {
  console.log(productos[i]);
}